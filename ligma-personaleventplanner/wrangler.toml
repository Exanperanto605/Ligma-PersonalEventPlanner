# REQUIRED: Cloudflare Pages project name
name = "ligma-personaleventplanner"

# REQUIRED: Build output directory for Pages + Next-on-Pages
pages_build_output_dir = ".vercel/output/static"

# REQUIRED: For Workers runtime features (e.g., node:buffer, async_hooks)
compatibility_date = "2025-11-25"

compatibility_flags = ["nodejs_compat"]